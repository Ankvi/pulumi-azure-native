{
    "name": "scripts",
    "private": true,
    "scripts": {
        "generate-packages": "bun index.ts build --no-submodules",
        "commit-and-push": "bun index.ts commit-and-push",
        "list-module-names": "bun index.ts list-module-names",
        "check-version": "bun index.ts check-version",
        "lint": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/node": "~20.12.8",
        "@types/bun": "^1.1.14",
        "@biomejs/biome": "1.9.4",
        "typescript": "^5.7.2"
    },
    "dependencies": {
        "@octokit/rest": "^21.0.2",
        "commander": "^12.1.0",
        "loglevel": "^1.9.2"
    }
}
